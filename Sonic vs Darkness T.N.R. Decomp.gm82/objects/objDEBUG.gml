#define Step_0
/*"/*'/**//* YYD ACTION
lib_id=1
action_id=603
applies_to=self
*/
            camera_object.x = floor(x) + look_shift_x;
            camera_object.y = floor(y) + look_shift_y;
            if keyboard_check(vk_left)
            x-=10
            if keyboard_check(vk_right)
            x+=10
            if keyboard_check(vk_up)
            y-=10
            if keyboard_check(vk_down)
            y+=10
